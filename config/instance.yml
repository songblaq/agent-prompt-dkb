instance:
  name: "agent-prompt"
  description: "Agent prompt curation instance"
  version: "0.2.0"

database:
  schema: "dkb"

storage:
  snapshots_dir: "./storage/snapshots"

upstream:
  ai_store_url: "http://localhost:8001"
