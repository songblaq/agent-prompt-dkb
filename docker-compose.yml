services:
  postgres:
    image: pgvector/pgvector:pg17
    environment:
      POSTGRES_USER: dkb
      POSTGRES_PASSWORD: dkb_dev
      POSTGRES_DB: agent_prompt_dkb
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
